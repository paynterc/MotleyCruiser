/// scr_room_objects()
// An array of objects available to use in room. When saving the room data, save the index value from this array, not the sprite name.

// Objects
var things = noone;

var i=0;
things[i]=obj_stage;

i=1;
things[i]=obj_table;

return things;
